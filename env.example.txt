# Configuração SMTP para envio de emails
# Renomeie este arquivo para .env e preencha com suas credenciais

# Servidor SMTP
SMTP_HOST=sh00122.hostgator.com.br

# Porta SMTP (465 para SSL/TLS, 587 para STARTTLS)
SMTP_PORT=465

# Email de autenticação (deve ser o mesmo que o email de destino)
SMTP_USER=upclinic8@gmail.com

# Senha do email (senha da conta de email no cPanel)
SMTP_PASSWORD=sua_senha_aqui

# Nota: Para produção, configure essas variáveis no painel do Vercel:
# Settings > Environment Variables

